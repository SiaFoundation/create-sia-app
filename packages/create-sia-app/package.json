{
  "name": "create-sia-app",
  "version": "0.1.16",
  "type": "module",
  "bin": {
    "create-sia-app": "./dist/index.js"
  },
  "files": [
    "dist/",
    "template/"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean && rm -rf ./template && cp -r ../../template ./template && rm -rf ./template/node_modules ./template/dist ./template/test-results",
    "dev": "tsup src/index.ts --format esm --watch"
  },
  "dependencies": {
    "@clack/prompts": "^0.10.0",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "tsup": "^8.4.0",
    "typescript": "~5.9.3"
  }
}
